language: c

compiler:
  - gcc
  #- clang

env:
  global:
    - foo=&

build:
  pre_ci:
    - echo 'true'
    - uname -a
    - echo 'hello'
    #- pwd
    

  ci:
    - echo "$foo"
    - ./configure && make && make install
    - ./hello
    
  post_ci:
    - echo "true"
