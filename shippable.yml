language: c

compiler:
  - gcc
  - clang

build:
  pre_ci:
    - echo 'true'
    - uname -a
    - echo 'hello'

  ci:
    - ./configure && make && make install
    - ./hello
    
  post_ci:
    - echo "true"
