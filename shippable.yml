language: c

compiler:
  - gcc
  #- clang
  
env:
  matrix:
    #- foo
build:
  pre_ci:
    - echo 'true'
    - uname -a
    - df -h
    #- echo 'hello'
    #- pwd
    

  ci:
    #- echo "$foo"
    #- echo "$foo2"
    - ./configure && make && make install
    - ./hello
    
  post_ci:
    - echo "true"
